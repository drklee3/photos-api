mutation SignupMutation(
  $username: String!
  $password: String!
  $email: String!
) {
  signup(username: $username, password: $password, email: $email) {
    user {
      id
      username
      email
    }
    token
  }
}
