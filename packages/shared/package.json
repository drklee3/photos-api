{
  "name": "@picatch/shared",
  "version": "1.0.0",
  "license": "AGPL-3.0-or-later",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "directory": "dist"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "yarn run clean && yarn run generate && tsc"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "trailingComma": "all"
  },
  "devDependencies": {
    "typescript": "4.5.2"
  }
}
